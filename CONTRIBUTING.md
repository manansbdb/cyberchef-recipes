# Contribuir

1. Adiciona um ficheiro JSON em `recipes/` com a receita CyberChef exportada.
2. Atualiza a tabela no `README.md`.
3. Abre um PR com uma descrição curta do caso de uso.

Formato esperado: array JSON de operações `{ "op": "Nome", "args": [...] }` (export do CyberChef).
